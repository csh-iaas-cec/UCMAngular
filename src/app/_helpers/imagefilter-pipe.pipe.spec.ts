import { ImagefilterPipePipe } from './imagefilter-pipe.pipe';

describe('ImagefilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagefilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
